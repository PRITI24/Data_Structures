public class main{
    public static void main(){
        Ilogger debugLogger = LoggerFactory.createLogger();
        Ilogger infoLogger = LoggerFactory.createLogger();
        Ilogger errorLogger = LoggerFactory.createLogger();

        debugLogger.log(msg:"This is a debug log msg";
        infoLogger.log(msg:"This is a debug log msg";
        errorLogger.log(msg:"This is a debug log msg";)
    }
}